% ============================================================
% Caption-Article Textual Similarity — VisualNews
% Overleaf: set compile root to paper/main.tex
% Compile: pdflatex -> bibtex -> pdflatex -> pdflatex
% ============================================================
\documentclass[11pt,a4paper]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[margin=2.5cm]{geometry}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{adjustbox}
\usepackage{multirow}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
\usepackage[numbers,sort&compress]{natbib}
\graphicspath{{../results/figures/}}
\setlength{\parskip}{4pt}

\title{\textbf{What Do Captions Actually Say?\\
Textual Similarity Between News Captions and Articles}}

\author{
  Author One \and Author Two \and Author Three\\
  School of Computer Science and Statistics, Trinity College Dublin
}
\date{}

\begin{document}
\maketitle

% ============================================================
\begin{abstract}
News image captions are expected to reflect the events described in their
accompanying articles, yet the degree of that alignment varies widely.
We quantify caption--article textual similarity across 2,520 pairs from the
VisualNews dataset \citep{liu2020visualnews} using three complementary metrics
(TF-IDF cosine, lemma-set Jaccard, and SBERT semantic similarity), and examine
how alignment relates to structural caption properties, named-entity overlap,
news category, and publication source.
Our results show that \ldots [fill in after final run].
\end{abstract}

% ============================================================
\section{Introduction}

A news caption and its article describe the same event, but from very
different vantage points: an article runs to hundreds of words; a caption
to roughly twenty.
Understanding how much article content a caption captures matters for
automatic caption generation, misinformation detection, and multimodal
summarisation.

We ask five questions: (RQ1) Do structural caption features predict
similarity? (RQ2) Does caption function affect alignment? (RQ3) Do named
entities provide a reliable alignment signal? (RQ4) Does similarity vary
by news category? (RQ5) Do UK and US outlets differ in caption--article
alignment?

Corresponding hypotheses: (H1) Structural features correlate significantly
with at least one similarity metric. (H2) Caption type significantly
predicts similarity. (H3) PERSON entity overlap is highest among entity
types. (H4) Similarity differs across news categories. (H5) UK outlets
(Guardian, BBC) show higher alignment than US outlets (WaPo, USA Today).

% ============================================================
\section{Data and Method}

\subsection{Dataset}
We draw 2,520 caption--article pairs from VisualNews \citep{liu2020visualnews},
covering four outlets (Guardian, BBC, Washington Post, USA Today) and six topic
categories. Table~\ref{tab:dataset} shows the distribution.

\begin{table}[h]
  \centering
  \caption{Dataset statistics per source.}
  \label{tab:dataset}
  \small
  \adjustbox{max width=\linewidth}{%
    \input{../results/figures/table_dataset_stats}}
\end{table}

\subsection{Similarity Metrics}
All text is preprocessed with spaCy \texttt{en\_core\_web\_trf}:
lowercased, lemmatised, stopwords and punctuation removed.
Articles are truncated to 512 tokens for TF-IDF and Jaccard, and to 128
tokens for SBERT (within the model's optimal range).

\begin{itemize}\setlength\itemsep{2pt}
  \item \textbf{TF-IDF cosine} ($s_{tf}$): sklearn \texttt{TfidfVectorizer}
    (unigrams + bigrams, $\text{min\_df}=2$, $\text{max\_df}=0.95$) fit on the
    combined caption + article corpus; per-pair cosine similarity.
  \item \textbf{Jaccard} ($s_{jac}$): $|C \cap A|\;/\;|C \cup A|$ on lemma
    sets; 0 if union is empty.
  \item \textbf{SBERT} ($s_{sbert}$): cosine similarity of
    \texttt{all-MiniLM-L6-v2} embeddings \citep{reimers2019sbert}.
\end{itemize}

\subsection{Feature Extraction}
Structural features (length, complexity, vocabulary) are extracted from
captions only using spaCy. POS-overlap features compare caption lemmas against
article lemmas per tag (NOUN, VERB, ADJ, PROPN). Named entities (PERSON, GPE,
ORG, DATE) are aligned with Levenshtein fuzzy matching (threshold 0.85,
\texttt{rapidfuzz}).

\subsection{Statistical Tests}
Pearson $r$ for correlations (RQ1); Kruskal--Wallis $H$ with
$\eta^2 = (H-k+1)/(n-k)$ for group comparisons (RQ2, RQ4, RQ5);
Dunn post-hoc with Bonferroni correction for pairwise source comparisons.

% ============================================================
\section{Results}

\subsection{Score Distributions}

Figure~\ref{fig:sim_dist} shows the three similarity distributions.
$s_{sbert}$ is right-shifted relative to $s_{tf}$ and $s_{jac}$, reflecting
its sensitivity to paraphrase rather than surface overlap.
Caption word counts are right-skewed with a median of $\approx 20$ words
(Figure~\ref{fig:cap_len}).

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{similarity_score_distributions.png}
  \caption{Distributions of TF-IDF, Jaccard, and SBERT similarity.
           Solid = mean; dashed = median.}
  \label{fig:sim_dist}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.75\linewidth]{caption_length_distribution.png}
  \caption{Caption word-count distribution (median annotated).}
  \label{fig:cap_len}
\end{figure}

% -------------------------------------------------------
\subsection{RQ1: Structural Features and Similarity}

Figure~\ref{fig:rq1_heat} shows Pearson $r$ between all structural features
and the three metrics (cells marked~* have $p < 0.05$).
Figure~\ref{fig:rq1_scatter} plots the three strongest significant
correlations with regression lines. Full values are in Table~\ref{tab:rq1}.

\textit{[Interpret: which features correlate, direction, magnitude.
Support or refute H1.]}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.72\linewidth]{rq1_correlation_heatmap.png}
  \caption{Pearson $r$ heatmap: structural features vs.\ similarity metrics.
           * $p < 0.05$.}
  \label{fig:rq1_heat}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{rq1_top_scatter.png}
  \caption{Top three significant feature--similarity correlations (95\% CI).}
  \label{fig:rq1_scatter}
\end{figure}

\begin{table}[h]
  \centering
  \caption{Pearson $r$ and $p$-values, sorted by $|r_{sbert}|$.
           * $p < 0.05$.}
  \label{tab:rq1}
  \small
  \adjustbox{max width=\linewidth}{%
    \input{../results/figures/table_rq1_correlations}}
\end{table}

% -------------------------------------------------------
\subsection{RQ2: Caption Function}

Caption type classification is not yet finalised
(\texttt{src/caption\_classifier.py} returns \textit{unclassified}
for all rows). RQ2 and H2 will be evaluated once the rule-based
classifier is implemented.

% -------------------------------------------------------
\subsection{RQ3: Named-Entity Alignment}

Figure~\ref{fig:rq3_bar} shows mean overlap rate per entity type.
Figure~\ref{fig:rq3_scatter} tests whether entity overlap predicts
semantic similarity. Table~\ref{tab:rq3} reports all entity metrics.

\textit{[Interpret: which entity type shows highest overlap. Whether
PERSON $>$ ORG/DATE (H3). Whether entity\_jaccard correlates with
sbert\_sim.]}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.82\linewidth]{rq3_entity_overlap_by_type.png}
  \caption{Mean entity overlap rate per type (error bars: 95\% CI).}
  \label{fig:rq3_bar}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.82\linewidth]{rq3_entity_jaccard_vs_sbert.png}
  \caption{Entity Jaccard vs.\ SBERT similarity, coloured by source.}
  \label{fig:rq3_scatter}
\end{figure}

\begin{table}[h]
  \centering
  \caption{Mean entity alignment metrics. Per-type coverage not available
           from pipeline output; shown for Overall row only.}
  \label{tab:rq3}
  \small
  \adjustbox{max width=\linewidth}{%
    \input{../results/figures/table_rq3_entity_metrics}}
\end{table}

% -------------------------------------------------------
\subsection{RQ4: News Category}

Figure~\ref{fig:rq4} shows similarity distributions across the six topic
categories. Table~\ref{tab:rq4} reports means, SDs, and Kruskal--Wallis
results.

\textit{[Interpret: significant differences? Which category highest/lowest?
Support or refute H4.]}

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{rq4_similarity_by_category.png}
  \caption{Similarity by news category.}
  \label{fig:rq4}
\end{figure}

\begin{table}[h]
  \centering
  \caption{Mean $\pm$ SD similarity by category. KW = Kruskal--Wallis.}
  \label{tab:rq4}
  \small
  \adjustbox{max width=\linewidth}{%
    \input{../results/figures/table_rq4_similarity_by_category}}
\end{table}

% -------------------------------------------------------
\subsection{RQ5: Cross-Source}

Figure~\ref{fig:rq5_sim} shows per-source similarity distributions.
Figure~\ref{fig:rq5_ent} shows mean entity Jaccard per source.
Table~\ref{tab:rq5} gives means and Kruskal--Wallis results;
Table~\ref{tab:rq5_dunn} gives Bonferroni-corrected pairwise $p$-values.

\textit{[Interpret: UK vs US difference. Which source has highest/lowest
alignment. Support or refute H5.]}

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{rq5_similarity_by_source.png}
  \caption{Similarity distributions by source.}
  \label{fig:rq5_sim}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.82\linewidth]{rq5_entity_jaccard_by_source.png}
  \caption{Mean entity Jaccard per source (95\% CI).}
  \label{fig:rq5_ent}
\end{figure}

\begin{table}[h]
  \centering
  \caption{Mean $\pm$ SD similarity by source. KW = Kruskal--Wallis.}
  \label{tab:rq5}
  \small
  \adjustbox{max width=\linewidth}{%
    \input{../results/figures/table_rq5_similarity_by_source}}
\end{table}

\begin{table}[h]
  \centering
  \caption{Dunn post-hoc pairwise $p$-values for SBERT similarity
           by source (Bonferroni corrected).}
  \label{tab:rq5_dunn}
  \small
  \adjustbox{max width=\linewidth}{%
    \input{../results/figures/table_rq5_dunn_posthoc}}
\end{table}

% ============================================================
\section{Conclusion}

We analysed caption--article textual similarity across 2,520 VisualNews pairs
using three metrics, structural features, entity alignment, and group
comparisons by category and source.
\textit{[Fill in: key findings, which hypotheses supported, practical
implications for caption generation and fact-checking.]}
Future work includes completing the caption function classifier (H2) and
scaling to the full dataset.

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
